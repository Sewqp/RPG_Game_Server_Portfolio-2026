# RPG Game Server Portfolio 강화 기획서

## 목표
면접관에게 실제 서비스 수준의 게임 서버를 보여주는 것

---

## Phase 1 — 더미 클라이언트 행동 다양화

### 목적
10,000명이 단순 패킷만 쏘는 게 아니라 실제 게임 유저처럼 행동하도록 개선

### 구현 범위

1. 가입부터 시작
   - 더미 클라이언트 실행 시 회원가입 → 로그인 → 게임 서버 접속 순서로 진행

2. 모험단 및 캐릭터 생성
   - 가입 후 모험단 생성
   - 캐릭터 생성 (직업, 닉네임 랜덤 배정)

3. 스탯 주기적 변경
   - 모든 클라이언트가 일정 주기로 PKT_CharacterStat 패킷 전송
   - 레벨, HP, MP 등 랜덤하게 변경

4. 맵 이동
   - 주기적으로 PKT_EnterRoom / PKT_LeaveRoom 패킷 전송
   - 던전 입장 → 일정 시간 후 퇴장 반복

5. 아이템 획득 및 레벨업
   - 일정 주기로 인벤토리 패킷 전송
   - 레벨업 시 스탯 변경 패킷 전송

---

## Phase 2 — 인증 서버 구축

### 목적
게임 서버 앞단에 인증 레이어 추가, 실제 서비스 구조에 가깝게 만들기

### 구현 범위

기본 기능
- 회원가입 (비밀번호 해시화 - bcrypt)
- 로그인 (JWT 토큰 발급)
- 로그아웃 / 토큰 만료

게임 특화
- 중복 로그인 방지 (같은 계정 동시 접속 차단)
- Redis 세션 관리 (토큰 저장 / TTL 설정)

보안
- 로그인 실패 횟수 제한 (brute force 방지)
- IP 차단

기술 스택: Node.js, JWT, bcrypt, Redis

---

## Phase 3 — 로컬 LLM 완전 연결 ✅ 완료 (2026-06-07)

### 목적
에러 발생 시 자동으로 LLM이 분석하고 Discord로 알람 전송

### 에러 파이프라인
게임 서버 에러 발생
→ AsyncLogger가 에러 로그 감지
→ 로컬 LLM(Qwen 2.5 Coder 14B)에 전송
→ LLM이 원인 분석 + 해결 방법 생성
→ Discord 봇으로 알람 전송

### 구현 완료 내용
- WinHTTP 기반 LM Studio(localhost:1234) POST 요청 (추가 DLL 없음)
- Discord 웹훅 HTTPS embed 전송
- `m_aiQueue` 분리 — 로거 스레드에서 AI 디스패치, IOCP 워커 블로킹 제거
- 60초 쿨다운 (에러 폭발 시 LLM 과호출 방지)
- `Configure()` 미설정 시 파이프라인 자동 비활성화

---

## Phase 4 — 시연 영상

### 구성
00:00 ~ 00:30  서버 구조 다이어그램 설명
00:30 ~ 01:00  더미 클라 10,000명 접속 테스트
01:00 ~ 01:30  에러 발생 → LLM 분석 → Discord 알람
01:30 ~ 02:00  Node.js 관리 서버 (랭킹, 거래소, 상태)

---

## 전체 일정

Phase 1  더미 클라 행동 다양화    1~2주   ⬜ 진행 예정
Phase 2  인증 서버 구축           1~2주   ⬜ 진행 예정
Phase 3  로컬 LLM 완전 연결      1주     ✅ 완료 (2026-06-07)
Phase 4  시연 영상 제작           3일     ⬜ 진행 예정

남은 기간: 3~4주
